//----------------------------------------------------------------------------------------------------
// models — the ready-made model components as the `xthree.models` table (`xnew(xthree.models.Chabudai)`)
// Each model builds its own default materials from xtextures, so every prop is optional.
//----------------------------------------------------------------------------------------------------

import { Chabudai } from './Chabudai';
import { Tatami } from './Tatami';
import { Carpet } from './Carpet';
import { Zabuton } from './Zabuton';

export const models = {
    Chabudai,
    Tatami,
    Carpet,
    Zabuton,
};
