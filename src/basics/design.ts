//----------------------------------------------------------------------------------------------------
// design — the caller-side decoration unit for internal parts of basics components
// Generated class names are page-unique, so page CSS cannot target a component's internal parts;
// components instead expose named parts that each accept a Design via their `designs` prop.
//----------------------------------------------------------------------------------------------------

export interface Design { className?: string; style?: string; }
